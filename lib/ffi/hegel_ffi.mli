(** Internal — native [libhegel] bindings, loader, and site paths. Not part of
    the public API; used by the Hegel library internally. *)

(**/**)

module Ffi = Ffi
module Loader = Loader
module Hegel_sites = Hegel_sites
